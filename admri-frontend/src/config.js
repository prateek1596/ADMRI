/**
 * ADMRI API Configuration
 * ───────────────────────
 * To enable the AI Clinical Support chat:
 *
 * 1. Go to https://console.anthropic.com
 * 2. Create an account and generate an API key
 * 3. Paste it below between the quotes
 *
 * Without a key the rest of the app (ML models, assessments,
 * patient management, notes) works perfectly fine.
 * Only the AI chat feature requires this key.
 *
 * ⚠️  Never share this file publicly or commit it to git.
 */

export const ANTHROPIC_API_KEY = ""; // ← paste your key here: "sk-ant-..."
